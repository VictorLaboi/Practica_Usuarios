<UserControl x:Class="Kanban.Views.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kanban.Views"
             mc:Ignorable="d" 
             d:DesignHeight="995" d:DesignWidth="1420"
             Background="White">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" HorizontalAlignment="Center" Width="1420">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="Home"  Width="97">
                <!--<MenuItem.Icon>
                    <Image Source="./Assets/img_home.png" Stretch="None" Margin="0,0,-13,-21" />
                </MenuItem.Icon>-->
            </MenuItem>
            <MenuItem Header="Tableros"  Width="97" Command="{Binding AddLayoutCommand}">
                <!--<MenuItem.Icon>
                    <Image Source="./Assets/img_home.png" Stretch="None" Margin="0,0,-13,-21" />
                </MenuItem.Icon>-->
            </MenuItem>
            <MenuItem Header="Administración"  Width="97">
                <!--<MenuItem.Icon>
                    <Image Source="./Assets/img_home.png" Stretch="None" Margin="0,0,-13,-21" />
                </MenuItem.Icon>-->
            </MenuItem>
        </Menu>
        <ScrollViewer Grid.Row="1">
            <!--AQUI VA EL CONTENIDO, SE TIENE QUE SACAR DE UN SOURCE DE LAYOUTS.-->
            <!--<ItemsControl>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>-->
        </ScrollViewer>
    </Grid>
</UserControl>
